# 页面设计分析模块

扫描设计稿/原型中的实际页面，将每个页面的元素布局如实记录为结构化描述。

## 核心原则

**只记录设计稿中实际存在的内容，禁止自行编造任何元素。**

> 这是最重要的原则。页面设计的价值在于忠实还原设计稿的每一个元素。如果设计稿中没有某个元素，就不要写。如果不确定是否存在，标注「[待确认]」而不是猜测。

**三大禁止**：
1. **禁止凭空编造**：不允许自己想象页面元素、搜索条件、表格列、弹窗字段
2. **禁止套用模板**：不允许用通用的列表页/弹窗模板填充，每个页面必须如实反映原稿
3. **禁止概括省略**：不允许把3个搜索条件概括为"若干搜索条件"

## 输出模板

```markdown
## 9. 页面设计

### 9.1 页面清单

| 序号 | 页面名称 | 页面类型 | 来源 |
|------|----------|----------|------|
| 1 | [页面名称] | 列表页/表单页/详情页/... | [设计稿具体位置] |
| 2 | [页面名称] | ... | ... |

---

### 9.2 [页面名称1]（列表页）

> 来源：[设计稿文件名/原型页面链接]

#### 1. 搜索条件

> 记录设计稿中搜索区域的所有条件。设计稿有几个就写几个，不要自行增减。

| 序号 | 搜索条件名 | 控件类型 | 说明 |
|------|-----------|----------|------|
| 1 | [从设计稿读取] | 输入框/下拉框/日期/... | [placeholder/默认值，从设计稿读取] |
| 2 | [从设计稿读取] | ... | ... |

搜索按钮：[从设计稿读取，如：查询、重置、展开更多]

#### 2. 表格列

> 记录设计稿中表格的所有列。设计稿有几列就写几列，不要自行添加列。

| 序号 | 列名 | 说明 |
|------|------|------|
| 1 | [从设计稿读取] | [列内容描述，从设计稿读取] |
| 2 | [从设计稿读取] | ... |
| ... | 操作 | [从设计稿读取操作按钮，如：编辑、删除] |

表格上方按钮：[从设计稿读取，如：新增、导出、批量删除]
表格下方：[从设计稿读取，如：分页、统计信息，没有就不写]

#### 3. 弹窗

> 记录设计稿中实际存在的弹窗。有几个弹窗就写几个。

##### 3.1 [弹窗名称]（如：新增用户弹窗）

弹窗标题：[从设计稿读取]
弹窗按钮：[从设计稿读取，如：确定/取消]

弹窗字段：

| 序号 | 字段名 | 控件类型 | 是否必填 | 说明 |
|------|--------|----------|----------|------|
| 1 | [从设计稿读取] | [从设计稿读取] | [从设计稿读取] | [从设计稿读取] |
| 2 | [从设计稿读取] | ... | ... | ... |

##### 3.2 [弹窗名称]（如：编辑用户弹窗）

*（同上结构）*

---

### 9.3 [页面名称2]（详情页/表单页/...）

> 来源：[设计稿文件名/原型页面链接]

#### 1. [区域名称]（如：基本信息）

| 序号 | 字段名 | 展示方式 | 说明 |
|------|--------|----------|------|
| 1 | [从设计稿读取] | 纯文本/标签/图片/... | [从设计稿读取] |

#### 2. [其他区域]

*（按设计稿实际区域划分）*

---

### 9.N 页面元素汇总

**新增/编辑共用弹窗差异**（仅当设计稿中存在时才记录）：
| 弹窗 | 新增字段 | 编辑独有字段 | 差异说明 |
|------|----------|-------------|----------|
```

## 分析步骤

1. **逐页扫描**：按设计稿中的页面顺序，一个页面一个页面地看
2. **如实记录**：设计稿中有什么元素就记录什么，没有的就不写
3. **保留原文**：字段名、按钮文字、列名保持设计稿中的原文
4. **标注来源**：每个页面标注来自哪个设计稿文件

## 不同页面类型的记录重点

### 列表页
重点记录：搜索条件（有哪些）→ 表格列（有几列）→ 操作按钮（表格上方的）→ 弹窗（点击按钮后出现的）→ 弹窗字段（弹窗里有几个输入项）

### 表单页
重点记录：表单字段（有哪些）→ 字段类型（输入框还是下拉等）→ 校验规则（必填/格式等）→ 提交按钮

### 详情页
重点记录：信息区块（分几块）→ 展示字段（有哪些）→ 展示方式（文本/标签/图片等）

### 弹窗/抽屉
重点记录：弹窗标题 → 表单字段（有几个、什么类型）→ 操作按钮

## 常见错误（必须避免）

| 错误 | 正确做法 |
|------|----------|
| 设计稿有2个搜索条件，自己编了5个 | 只写设计稿中的2个 |
| 设计稿没有弹窗，自己加了一个"新增弹窗" | 设计稿没有就不写 |
| 把通用的列表页模板直接套用 | 每个页面必须从设计稿中逐个读取元素 |
| 搜索条件写成"若干搜索条件" | 逐个列出每个搜索条件的名称和类型 |
| 弹窗字段写成"包含基本信息字段" | 逐个列出每个字段的名称、控件类型、是否必填 |
| 设计稿中没有标注必填，自己标记了必填 | 没有明确信息的标注「[待确认]」 |

## 输出位置

**追加到主文档**：`docs/product-design/[人名-编号-项目名]/产品设计审查报告.md`

在主文档中以 `## 9. 页面设计` 章节形式呈现，**不生成独立文件**。

| 分析模块 | 主文档章节 |
|----------|------------|
| 页面清单 | 9.1 页面清单 |
| 各页面详情 | 9.2 ~ 9.N 页面详情 |
| 元素汇总 | 9.N+1 页面元素汇总 |
