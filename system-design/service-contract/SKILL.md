---
name: system-design-service-contract
description: 分析服务间的契约定义。当用户提供架构文档，需要梳理"服务间的接口约定"时触发。TRIGGER 当用户说"定义下服务契约"、"梳理下接口约定"、"服务间的合同是什么"时使用。
---

# 服务契约分析 Skill

基于用户提供的文档,分析并输出标准化的服务契约文档。

## 输出路径

```
docs/system-design/[项目名]/服务契约.md
```

## 输出模板

```markdown
# 服务契约文档

> 文档生成时间：YYYY-MM-DD
> 数据来源:[文件名]

---

## 1. 契约概述

| 项目 | 内容 |
|------|------|
| **服务提供者** | [服务A] |
| **服务消费者** | [服务B, 服务C] |
| **契约类型** | 同步API / 异步消息 |
| **版本** | v1.0.0 |

---

## 2. 同步API契约

### 2.1 API清单
| API | 方法 | 路径 | 功能 | 版本 |
|-----|------|------|------|------|
| [API1] | GET/POST/PUT/DELETE | [路径] | [功能] | v1 |
| [API2] | GET/POST/PUT/DELETE | [路径] | [功能] | v1 |

### 2.2 请求结构
#### [API名称]
```json
{
  "field1": "type",
  "field2": "type"
}
```
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| field1 | string | 是 | [说明] |
| field2 | integer | 否 | [说明] |

### 2.3 响应结构
#### [API名称]
```json
{
  "code": 0,
  "data": {},
  "message": "success"
}
```
| 字段 | 类型 | 说明 |
|------|------|------|
| code | integer | 状态码 |
| data | object | 数据体 |
| message | string | 消息 |

### 2.4 错误码定义
| 错误码 | 含义 | 场景 |
|--------|------|------|
| 0 | 成功 | 正常返回 |
| 400 | 参数错误 | 参数校验失败 |
| 401 | 未授权 | 未登录/Token失效 |
| 403 | 禁止访问 | 权限不足 |
| 404 | 不存在 | 资源未找到 |
| 500 | 服务错误 | 内部错误 |

---

## 3. 异步消息契约

### 3.1 事件清单
| 事件 | 类型 | Topic | 生产者 | 消费者 |
|------|------|-------|--------|--------|
| [事件1] | 领域事件/集成事件 | [topic] | [服务] | [服务] |
| [事件2] | 领域事件/集成事件 | [topic] | [服务] | [服务] |

### 3.2 事件结构
#### [事件名称]
```json
{
  "eventId": "uuid",
  "eventType": "OrderCreated",
  "timestamp": 1234567890,
  "data": {}
}
```
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| eventId | string | 是 | 事件唯一ID |
| eventType | string | 是 | 事件类型 |
| timestamp | long | 是 | 时间戳 |
| data | object | 是 | 事件数据 |

### 3.3 消费语义
| 消费者 | 事件 | 消费语义 | 说明 |
|--------|------|----------|------|
| [服务] | [事件] | 至少一次/精确一次 | [说明] |

---

## 4. 数据契约

### 4.1 共享数据结构
| 数据 | 字段 | 类型 | 说明 |
|------|------|------|------|
| [数据1] | [字段] | [类型] | [说明] |
| [数据2] | [字段] | [类型] | [说明] |

### 4.2 枚举定义
| 枚举 | 值 | 说明 |
|------|------|------|
| Status | ACTIVE | 激活 |
| Status | INACTIVE | 未激活 |
| Status | DELETED | 已删除 |

---

## 5. 版本策略

### 5.1 版本规则
| 类型 | 规则 | 示例 |
|------|------|------|
| URL版本 | /v1/ /v2/ | /v1/users |
| 参数版本 | 版本字段 | version=v1 |
| Header版本 | Accept头 | Accept: application/vnd.api.v1+json |

### 5.2 兼容性保证
| 变更类型 | 兼容性 | 说明 |
|----------|----------|------|
| 新增字段 | 兼容 | 老版本可忽略 |
| 删除字段 | 不兼容 | 需要大版本升级 |
| 修改字段 | 视情况 | 可能不兼容 |

---

## 6. 契约测试

### 6.1 契约测试覆盖
| 声明方 | 嶈费方 | 测试状态 |
|----------|--------|----------|
| [服务A] | [服务B] | ✅/❌ |
| [服务A] | [服务C] | ✅/❌ |

---

## 7. 信息完整性检查

**缺失项**：[列出所有待补充项目]
```

---

## 执行要求

1. 读取用户提供的文档
2. 识别服务契约信息
3. 按模板输出文档到 `docs/system-design/[项目名]/服务契约.md`
4. 标注缺失信息并提示用户
